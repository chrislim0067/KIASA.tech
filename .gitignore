node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Environment files.
#
# `.env*.local` alone left a gap: a plain `.env`, `.env.production` or
# `.env.development` would have been committed — into a public repository. The
# example file is the one deliberate exception, and carries names only.
.env
.env.*
!.env.example

# Verification artefacts
verify-shots/
.verify-profile/

# Archived copy of the pre-rebrand original. Kept locally as the extraction
# source (npm run extract reads it); deliberately not published.
/legacy/

# Promo film renders (npm run video:render). Reproducible from video/, not source.
video/out/
